# Database Module

This directory manages database connections, schemas, migrations, and database models.

## Structure
- `models/`: Database models (e.g., SQLModel or SQLAlchemy tables).
- `migrations/`: Alembic migrations.
- `connection.py`: Database connection setup.
